// Release build — RevenueCat Production API key
// Replace the placeholder below with your actual key from:
//   app.revenuecat.com → Project Settings → API Keys → Public key (Live)
REVENUECAT_API_KEY = appl_REPLACE_WITH_YOUR_PRODUCTION_KEY
